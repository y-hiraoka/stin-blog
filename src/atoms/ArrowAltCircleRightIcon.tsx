import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import { faArrowAltCircleRight } from "@fortawesome/free-solid-svg-icons";

export const ArrowAltCircleRightIcon: React.VFCX = props => {
  return <FontAwesomeIcon className={props.className} icon={faArrowAltCircleRight} />;
};
