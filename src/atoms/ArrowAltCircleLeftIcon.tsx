import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";
import { faArrowAltCircleLeft } from "@fortawesome/free-solid-svg-icons";

export const ArrowAltCircleLeftIcon: React.VFCX = props => {
  return <FontAwesomeIcon className={props.className} icon={faArrowAltCircleLeft} />;
};
